# Cherkas Timofey

## Contacts:

- **Phone number:** +375293535442  
- **E-mail:** cc.timofey@mail.ru  
- **Telegram:** [@Uwannatell](https://t.me/Uwannatell)  
- **Instagram:** [ll.timofey](https://www.instagram.com/ll.timofey/)  

---

**Student** majoring in ==computer science and computer engineering==. Looking for an opportunity to _improve my skills_.

---

## Skills:

- Git *(basic)*
- C#
- HTML, CSS

---

## Code examples:

### C# Console Applications
```csharp
// Program 1: Check if number is positive and even
internal class NumberChecker
{
    static void Main(string[] args)
    {
        Console.Write("Enter a number: ");
        int number = int.Parse(Console.ReadLine()!);
        
        if (number > 0 && number % 2 == 0)
        {
            Console.WriteLine("Yes");
        }
        else
        {
            Console.WriteLine("No");
        }
    }
}

// Program 2: Find perfect numbers
internal class PerfectNumberFinder
{
    static void Main(string[] args)
    {
        Console.Write("Enter k: ");
        int k = int.Parse(Console.ReadLine()!);

        for (int i = 1; i <= k; i++)
        {
            if (IsPerfectNumber(i))
            {
                Console.WriteLine($"{i} - perfect number");
            }
            else
            {
                Console.WriteLine($"{i} - not a perfect number");
            }
        }
    }

    public static bool IsPerfectNumber(int number)
    {
        if (number < 2) return false;
        
        int sum = 0;
        for (int i = 1; i <= number / 2; i++)
        {
            if (number % i == 0)
            {
                sum += i;
            }
        }
        
        return sum == number;
    }
}

---

## Completed projects:

- CV

---

## Courses and trainings:

- Phyton course from LetPy
- ~~JS course from Rolling Scopes~~
- ~~React course from KTS~~

---

## English level:

A1
